meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 17
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: out/artifacts/testtask_jar/testtask.jar
  persistence_mount: /data
  container_port: 80
